-- Phase 2.1A: Supabase Project Foundation
--
-- Intentionally empty. No tables exist yet — this migration only
-- establishes a baseline so the migration history starts at a known
-- point. Real schema (profiles, creators, conversations, messages,
-- purchases, notifications, etc.) is added in later migrations as each
-- corresponding repository in lib/repositories/supabase/ is implemented.

select 1;
